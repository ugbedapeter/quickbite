name: Quick bite

on:
  push:
    branches:
      - main
      - 'release/**'
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.35.6' # Use a more recent, stable version of Flutter
          channel: 'stable'

      - name: Install dependencies
        run: flutter pub get
    

      - name: Build APK
        run: flutter build apk --release

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: quickbite-apk
          path: build/app/outputs/flutter-apk/app-release.apk
